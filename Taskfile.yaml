version: '3'

includes:

  sandbox: ./taskfiles/Taskfile_sandbox.yml
  py: ./taskfiles/Taskfile_pelican.yml

tasks:

  default:
    desc: lists all tasks
    silent: true
    cmds:
      - task --list-all
      - echo ""
      - echo "Example usage-> 'task check-virtualenv'"